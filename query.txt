SELECT 
    c.first_name, 
    d.department_name
FROM 
    employees AS c
JOIN 
    departments AS d 
    ON c.department_id = d.id
WHERE 
    c.salary > 50000 
    AND d.location = 'NYC'
ORDER BY 
    c.last_name ASC;